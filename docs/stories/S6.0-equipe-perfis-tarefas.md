# S6.0 — Equipe: Perfis, Gestão e Distribuição de Tarefas

**Estado:** done
**Modo:** Deep (incremental por fases)
**Origem:** Pedido do usuário (gestão de equipe + perfil individual + Kanban de tarefas + metas)

## Description
Ambiente completo de equipe: cada usuário vê seu perfil (função, responsabilidades,
escala, tarefas, metas, evolução) e admins/super_admins gerenciam a equipe como um
todo, com distribuição de tarefas (Kanban) e metas individuais/de equipe.

## Decisões de modelo (aprovadas)
- **Vínculo:** `equipe.perfil_id` liga conta ↔ membro da escala (1:1 por casa)
- **Tarefas:** nova tabela `tarefas` + Kanban (admin distribui a fazer/fazendo/concluída)
- **Metas:** nova tabela `metas` numéricas (admin define por pessoa/equipe) + evolução no tempo
- **Entrega:** incremental — 3 fases, cada uma um PR testado

## Fase 1 — Perfil & Gestão de Equipe ✅
- [x] Migration 0006: `equipe.perfil_id` (FK perfis, nullable, ON DELETE SET NULL)
- [x] `roles.ts`: ROLE_LABEL + ROLE_RESPONSABILIDADES + helpers; rota `/perfil` liberada
- [x] `/perfil` (self): cabeçalho, responsabilidades, módulos com acesso, próximos turnos
- [x] `/admin`: vincular conta ↔ membro da equipe (action `vincularPerfilEquipe` + select)
- [x] Nav: `/perfil` no sidebar e bottom-nav
- [x] Testes: validação/permissão de `vincularPerfilEquipe`

## Fase 2 — Tarefas & Kanban ✅ (PR #39 · `fd79cc8`)
- [x] Migration 0007: tabela `tarefas` (titulo, descricao, perfil_id, status, prioridade, prazo, casa) — aplicada em produção (10 colunas)
- [x] Actions: criar/atribuir/mover/concluir tarefa (máquina a_fazer → fazendo → concluida) — `actions/tarefas.ts`
- [x] Kanban board no admin (distribuição) + "minhas tarefas" no perfil — `components/tarefas/`
- [x] Testes da máquina de estados — `lib/__tests__/state-machine.test.ts` + `actions/__tests__/tarefas.test.ts`

## Fase 3 — Metas & Evolução ✅ (PR #40 · `5061b6f`)
- [x] Migration 0008: tabela `metas` (escopo individual/equipe, perfil_id, alvo, atual, periodo) — aplicada em produção (14 colunas)
- [x] Actions: definir meta, atualizar progresso
- [x] UI: metas no perfil (individual + equipe) + evolução no tempo — `admin/metas/page.tsx`
- [x] Painel de metas da equipe no admin

## Notas
- Cada fase com migration pede aplicação no banco de produção (consentida no fluxo).
- Bottom-nav fica com 9 itens p/ admin — avaliar overflow/“mais” como follow-up.
