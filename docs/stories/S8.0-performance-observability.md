# S8.0 — Performance & Observability (Self-Hosted)

**Estado:** done
**Modo:** Deep (incremental por fases)
**Origem:** Pedido do usuário — instrumentar a operação sem dependência externa (zero SaaS)

## Description
Dar olhos à operação BiCA/AMP sem trazer SaaS. Tudo self-hosted no Supabase:
captura de eventos de uso, rastreamento de erros persistido, baseline de
performance (Web Vitals reais + Lighthouse) e painel de saúde no admin.

## Decisões de modelo (aprovadas)
- **Sem SaaS:** zero Sentry, zero PostHog. Lighthouse CI + bundle-analyzer (open-source) OK.
- **Uma tabela:** `analytics_events` cobre eventos, erros e web vitals (via `status`/`value`/`error_message`).
- **Wrapper:** `withAnalytics()` instrumenta Server Actions sem refactor (captura duração + erro).
- **Entrega:** Fases 1–3. Fase 4 (analytics de produto/dashboards) é follow-up S9+.

## Fase 1 — Observability Foundation ✅ (PR #46)
- [x] Migration 0009: tabela `analytics_events` (RLS relaxada padrão 0002) — aplicada em produção via MCP (10 colunas verificadas)
- [x] Estender `lib/logger.ts` (estruturado: ts/level/msg/context, mantendo API + `logger.child()`)
- [x] `lib/analytics.ts` — `trackEvent()` server-side (fire-and-forget, nunca lança)
- [x] `actions/instrumented.ts` — `withAnalytics()` wrapper (duração + sucesso/erro, re-lança)
- [x] Aplicar em 3 ações-protótipo: `reserva.criar`, `ficha.criar`/`ficha.editar`, `checklist.marcar_item`
- [x] Tipo `analytics_events` em `database.types.ts`
- [x] Testes Vitest do `withAnalytics` (294 GREEN)

## Fase 2 — Performance & Web Vitals ✅ (commit `c301e1d`)
- [x] `web-vitals-reporter.tsx` (client `useReportWebVitals` → Server Action `registrarWebVital`) montado no layout autenticado
- [x] `next.config.ts` — image optimization (avif/webp)
- [x] `@next/bundle-analyzer` + script `analyze`
- [x] `@lhci/cli` + `.lighthouserc.json` (raiz) + workflow `.github/workflows/lighthouse.yml`

## Fase 3 — Error Tracking & Painel de Saúde ✅
- [x] Estender instrumentação às demais actions: `tarefa.criar`/`mover`, `meta.criar`/`atualizar_progresso`, `estoque.criar_item`/`atualizar_quantidade`, `compra.abrir_rodada`/`fechar_rodada`, `escala.salvar`
- [x] `lib/analytics-queries.ts` (erros recentes, ações lentas, vitals P75, resumo diário) + `ratingWebVital`/`percentil` (puras, testadas)
- [x] Painel `/admin/saude` (KPIs, Web Vitals P75, volume diário, ações lentas, erros) com design v3 + link no `/admin`
- [x] Testes Vitest de `percentil` e `ratingWebVital` (305 GREEN)
- [ ] Alerting push (e-mail/WhatsApp via Edge Function + pg_cron) → **follow-up S9+**
- [ ] `get_advisors`/`get_logs` como rotina periódica → **follow-up operacional (manual via MCP)**

## Notas
- Migrations na raiz `supabase/` NÃO são auto-aplicadas (só `app/supabase`) → MCP `apply_migration` + `list_tables`.
- Next.js 16 tem breaking changes — ler `app/node_modules/next/dist/docs/` antes de Web Vitals/config.
- Cada fase = um PR testado (lint+test+typecheck verde).
