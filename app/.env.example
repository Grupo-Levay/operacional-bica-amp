# Supabase — projeto ducbzdfxzaifzqefolhy
# Disponíveis no painel: Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-public-key>

# URL base do app (usada em redirects de e-mail, ex: reset de senha).
# Em produção/preview o Vercel deve definir isto. Em dev local:
NEXT_PUBLIC_SITE_URL=http://localhost:3000
